# 7. 第一次视觉跑偏

输出：`e2e_demo/Hy3D_textured_00006_deepseek`  
耗时：14.8s（视觉约 3.1s）

视觉回报：

- `view_3` 正面（“一只眼和嘴”）
- `view_0` 背面
- `opening_center = (0.5, 0.55)` ← 提示词示例被原样抄回
- 转到模型后意图点约 `(0, 130)` mm

实际开孔：`(8.0, 44.7)` mm，更靠下。

根因：

1. 5 万面后水平 PCA 把模型拧了 **82°**（原网格是 180°）
2. 视觉在侧视上判正反，不可信
3. JSON 示例里的 `u=0.5,v=0.55` 被模型照抄
4. 几何搜索又丢掉了视觉点，改选更低的候选
